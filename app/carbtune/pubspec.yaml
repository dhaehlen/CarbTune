name: carbtune
description: CarbTune — Carburetor Pressure Monitor & Engine Tuning Tool
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  # flutter_blue_plus: ^1.31.15  # Added when BLE integration begins (MA-01a)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
